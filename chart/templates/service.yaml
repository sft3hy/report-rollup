apiVersion: v1
kind: Service
metadata:
  name: {{ include "report-rollup.fullname" . }}
  labels:
    {{- include "report-rollup.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.portWeb }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "report-rollup.selectorLabels" . | nindent 4 }}
